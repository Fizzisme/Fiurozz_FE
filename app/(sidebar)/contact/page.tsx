import Contact from '@/views/Contact';

export const metadata = {
    title: 'Contact Us',
    description: 'Get in touch with the team behind Fiurozz — report a bug, share an idea, or say hello.',
};

export default function ContactPage() {
    return <Contact />;
}
